#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define number 30
int main()
{
    int i, j, k;
    int a[30];
    int map[number];
    for (i = 0; i < number; i++)
    {
        map[i] = -1;
    }
    j = 0;
    srand(time(NULL));
    while (j < 30)
    {
        k = rand() % 30;
        if (map[k] != -1)
            continue;
        map[k] = k;
        a[j] = k;
        j = j + 1;
    }
            printf("%s ", a[i]);
     /*   static char a [30];
         a [1]='?¨ª??',a [2]="???¡ã??",a [3]="?¨´???¨²",a [4]="???¡è??",a [5]="????¡¤?";
         a [6]="???¨²",a [7]="??????",a [8]="??¡Á???",a [9]="??????",a [10]="???¨º??";
         a [11]="??????",a [12]="?¡§????",a [13]="????",a [14]="?¡¤?¨¬",a [15]="???¡ì";
         a [16]="??¨¨??h",a [17]="??????",a [18]="???¡è??",a [19]="??????",a [20]="???¡Â??";
         a [21]="??????",a [22]="??????",a [23]="????¨¨?",a [24]="??????",a [25]="????¨¨?";
         a [26]="¡¤??¡è",a [27]="??????",a [28]="?????¡ã",a [29]="????",a [30]="???¡Á??";*/
    for (i = 0; i < 30; i++)
    {
        if (i % 10 == 0)
        {
            printf("\n");
        }
        printf("%s ", a[i]);
    }
    return 0;
}